// Navigate to another page when a menu item or card is clicked
function goToPage(page) {
    window.location.href = page + "announcementadmin.php"; // Redirect to the respective page (e.g., dashboard.html, user.html)
  }
  
  function goToManagePage(section) {
    window.location.href = section + "announcementadmin.php"; // Redirect to the management page for each section
  }
  
